<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="dev.rikka.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="dev.rikka.shizuku.manager.permission.API_V33" />

    <uses-sdk tools:overrideLibrary="rikka.shizuku.api;rikka.shizuku.provider" />

    <application
        android:name=".SpeedCoolApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:label="SpeedCool"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpeedCool">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SpeedCool">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.SpeedCoolService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <service
            android:name="dev.rikka.shizuku.api.ShizukuService"
            android:exported="true"
            android:permission="dev.rikka.shizuku.manager.permission.API_V23">
            <intent-filter>
                <action android:name="dev.rikka.shizuku.api.ACTION_START_SERVICE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
